# Текущий статус PROBA

Последняя проверка: 29 июля 2026 года.

## Работает

- активный production-релиз: `20260729-042344`, исходный commit: `552c31e`;
- production-сборка размещена в отдельном каталоге VPS;
- `https://probaclinical.ru` и `https://www.probaclinical.ru` отвечают `200`;
- A-записи apex и `www` опубликованы на всех авторитетных NS Timeweb и видны через публичные резолверы Google и Cloudflare;
- сертификаты Let's Encrypt для apex и `www` действительны;
- временный адрес `https://proba.64-188-58-140.nip.io` сохранён как технический резерв;
- JavaScript, CSS и SPA fallback отвечают `200`;
- глобальная кнопка Telegram `Открыть PROBA` направлена на `https://probaclinical.ru`;
- Main Mini App включён и через BotFather направлен на `https://probaclinical.ru`; Bot API возвращает `has_main_web_app: true`;
- отдельный Direct Mini App `https://t.me/probaclinicalbot/proba` создан в BotFather, направлен на `https://probaclinical.ru` и использует адаптированную обложку 640×360 px;
- кнопка `Открыть PROBA` визуально проверена в чате `@probaclinicalbot`;
- при запуске запрашивается Telegram Fullscreen, контентные вертикальные свайпы закрытия отключаются;
- собственные стрелки назад удалены со всех production-экранов: при наличии истории показывается системный `Telegram.WebApp.BackButton`, на корневом экране остаётся системная кнопка закрытия, возврат свайпом от левого края сохранён;
- production-интерфейс открывается без декоративной рамки телефона и занимает всю доступную область Mini App;
- верхняя панель и домашний экран учитывают Telegram safe area, поэтому элементы не пересекаются с системными кнопками закрытия и меню;
- адаптивная раскладка автоматически перестраивается на ширинах 320, 393 и 768 px; автоматические проверки мобильного runtime проходят полностью;
- брендовая карточка Open Graph/X размером 1200×630 px опубликована на основном домене; social-ссылка `/open.html` показывает большое превью и переводит пользователя в Direct Mini App `/proba`;
- описание и команды `@probaclinicalbot` настроены;
- соседние проекты `claude.applaza.ru` и `text2pdf.ru` после изменений отвечают `200`.

## Завершено

Миграция с временного адреса на брендовый домен завершена. DNS, TLS, сайт, ассеты, SPA, Telegram menu button и Main Mini App проверены; временный DNS-мониторинг больше не требуется.
