# Текущий статус PROBA

Последняя проверка: 29 июля 2026 года.

## Работает

- активный production-релиз: `20260729-042003`, исходный commit: `55928a4`;
- production-сборка размещена в отдельном каталоге VPS;
- временный адрес `https://proba.64-188-58-140.nip.io` отвечает `200`;
- сертификат Let's Encrypt действителен;
- JavaScript, CSS и SPA fallback отвечают `200`;
- глобальная кнопка Telegram `Открыть PROBA` направлена на работающий HTTPS-адрес;
- Main Mini App включён в BotFather; Bot API возвращает `has_main_web_app: true`;
- кнопка `Открыть PROBA` визуально проверена в чате `@probaclinicalbot`;
- при запуске запрашивается Telegram Fullscreen, контентные вертикальные свайпы закрытия отключаются;
- production-интерфейс открывается без декоративной рамки телефона и занимает всю доступную область Mini App;
- верхняя панель и домашний экран учитывают Telegram safe area, поэтому элементы не пересекаются с системными кнопками закрытия и меню;
- адаптивная раскладка автоматически перестраивается на ширинах 320, 393 и 768 px; автоматические проверки мобильного runtime проходят полностью;
- брендовая карточка Open Graph/X размером 1200×630 px опубликована; social-ссылка `/open.html` показывает превью и переводит пользователя в Main Mini App;
- описание и команды `@probaclinicalbot` настроены;
- соседние проекты `claude.applaza.ru` и `text2pdf.ru` после изменений отвечают `200`.

## Ожидается

Основной домен `probaclinical.ru` зарегистрирован, а записи apex и `www` уже настроены на `64.188.58.140`. Первичная зона `.ru` пока не опубликована авторитетными DNS-серверами и снаружи возвращает `NXDOMAIN`.

После публикации зоны автоматическая проверка должна:

1. дождаться сертификата Caddy для apex и `www`;
2. проверить основной домен, ассеты и SPA;
3. повторно проверить соседние проекты;
4. переключить Telegram menu button и Main Mini App с временного адреса на `https://probaclinical.ru`;
5. обновить этот документ и отключить мониторинг.
